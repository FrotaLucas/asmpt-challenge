

steps:
    - task: UseNode@1
      displayName: install node
      inputs:
        version: '20.0'

    - script: |
        echo "hellow intermediate"
      displayName: intermediate script

    - task: Npm@1
      displayName: dowload dependencies 
      inputs:
        command: install
        workingDir: $(FrontendDirectory)