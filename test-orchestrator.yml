trigger: none

pool:
  vmImage: 'ubuntu-latest'

variables:
  FrontendDirectory: "Frontend"

stages:
    - stage: First_Stage
      jobs:
          - job: RunFrontend
            displayName: JobFrontend
            steps:
              - checkout: self
              - template: Frontend/test-frontend-tamplate.yml

    - stage: Second_Stage
      jobs:
          - job: RunBackend