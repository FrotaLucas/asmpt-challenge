trigger:
  - main

pool:
  vmImage: 'Defaul'  

variables:
  BuildConfiguration: 'Release'
  solutionPath: 'Backend/asmpt-challenge.sln'

steps:
- checkout: self

- script: |
    echo "Hello"
  displayName: 'Print Hello'