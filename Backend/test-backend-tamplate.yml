

steps:
    - script: |
        echo "hellow template backend"
      displayName: script 
      
    - task: DotNetCoreCLI@1
      displayName: install dotnet
      inputs:
          command: build
          projects: $(SolutionPath)

    - task: DotNetCoreCLI@1
      displayName: Restore
      inputs:
          command: restore
          projects: $(SolutionPath)

    - task: DotNetCoreCLI@1
      displayName: Build
      inputs:
          command: build
          projects: $(SolutionPath)